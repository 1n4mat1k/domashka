#include <stdio.h>
int main()
{
   int n;
   printf("Введите целое число N: ");
   scanf ("%d", &n);
   int k = 0, sum = 0;
   while (sum < n) {
       ++k;
        sum += k;
   }
   printf("Число K равно %d, и сумма равна %d", k, sum);
   return 0;
}
