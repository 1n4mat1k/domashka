// Доделать







#include <stdio.h>

int main()
{
    int n, k, i, s;
    printf("Введите число N (день недели) когда оно было 1 января: ");
    scanf("%d", &n);
    
    printf("Введите число K: ");
    scanf("%d", &k);
    
    while (k > 7){
        k = k - 7;
    }
    k = k + 7 + 1 - n;
    s = 
    printf("Номер дня недели: %d", k);

    return 0;
}
