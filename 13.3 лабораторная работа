#include <stdio.h>
int main() {
    int n, a, b, i;
    int c = 1;
    printf("Введите число N: ");
    scanf("%d", &n);
    b = 1;
    for (i = 0; i < n - 1; i++){
        c += 2;
        b += c;        
    }
    printf ("Сумма: %d \n", b);
    return 0;
}
